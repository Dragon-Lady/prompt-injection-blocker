{
  "name": "prompt-injection-blocker",
  "version": "0.1.0",
  "description": "Read-only scanner for prompt-injection and LLM anti-analysis text in files before agent review.",
  "license": "MIT",
  "type": "commonjs",
  "bin": {
    "prompt-injection-blocker": "./bin/prompt-injection-blocker.js"
  },
  "scripts": {
    "scan": "node bin/prompt-injection-blocker.js",
    "smoke": "node bin/prompt-injection-blocker.js --help",
    "test": "node --check bin/prompt-injection-blocker.js && node --check src/scanner.js && node test/smoke.js"
  },
  "engines": {
    "node": ">=18"
  }
}
